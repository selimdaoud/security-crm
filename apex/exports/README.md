# APEX exports

- `sed-dashboard-2/` is the current full split application export.
- `legacy/` contains older page-level exports retained for comparison.

Treat exports as deployment snapshots. After changing the application or a
source fragment, create a fresh full export and review the generated diff.

